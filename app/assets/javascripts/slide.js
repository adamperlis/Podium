$("#delete_slide").click(function(e) {
	/*
	var idOfSlide = e.target.val("data-slide-number")
	$.post("/slides/" + idOfSlide + "/destroy",
		success: function() {
			do slide removy things
		}
	)*/

	console.log('clicked delete slide')
	console.log(e)
});
