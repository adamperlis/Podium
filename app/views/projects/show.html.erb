<div class: "row">
	<div class="slideshow">

			<%= image_tag @project.image (:thumb).to_s %>
			<p>
				<%= @project.description %>
			</p>
			<p>
				<%= @project.user.name %>
			</p>
			<% if current_user == @project.user %>

	<div id="controls">
		<form id="formControls">
			<input type="button" class="button" id="previous" value="PREVIOUS"/>
			<input type="button" class="button" id="next" value="NEXT"/>
		</form><!-- #formControls -->
		<%= image_tag("podium_logo.png", class: "logo_white") %>
	</div><!-- #controls -->
			
		<%= link_to 'Edit', edit_project_path(@project) %> |
		<% end %>
		<%= link_to 'Back', projects_path %>
	</div>
</div>


