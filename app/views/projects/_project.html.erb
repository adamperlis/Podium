
		

			<div class="col-md-4 col-sm-4">
				<div class="thumbnail deck">
					<% if project.slides.first %>
						<% if project.slides.first.mimetype == "video/mp4" %>
							<%= link_to project do %>
								<video width='100%' height='100%'>
									<source src="<%= project.slides.first.filepicker_url_thumb %>">
								</video>
							<% end %>
						<% else %>
							<% unless project.slides.first.filepicker_url.blank? %>
								<%= link_to (image_tag project.slides.first.filepicker_url_thumb), project, :class => 'deck-thumb img-responsive' %> 
							<% else %>
								<%= link_to project do %>
									<div class="blank" id="empty"></div>
								<% end %>
								<script> 
								$(document).ready(function(){
									generateThumb("<%= project.slides.first.embed_code -%>", "<%= project.slides.first.id -%>")
								});
								</script>
							<% end %>
						<% end %>
				  <% else %>
					  <div class="untitled-project">
					  	<i class="entypo-picture" id="untitled-icon"></i>
					  </div>
					<% end %>
		
			<div class="caption project-description">
				<p>
			    <%= link_to project.description, project %> 
			    <span class="updated-at">Updated: <%= localize(project.updated_at, :format => :long) %></span>
		    </p>
	    </div>
			    <ul class="project-tools">
			    		<li><%= link_to(project, method: :delete, data: { confirm: 'Are you sure?' }) do %>
							<span class="delete"><i class="entypo-cancel" data-tooltip data-options="disable-for-touch:true" data-tooltip class="has-tip tip-bottom" title="Delete"></i></span>
							<% end %></li>
		          <li><%= link_to(edit_project_path(project)) do %>
							<span class="edit"><i class="entypo-pencil" data-tooltip data-options="disable-for-touch:true" data-tooltip class="has-tip tip-bottom" title="Edit"></i></span>
							<% end %></li>
					</ul>
    	</div>
   	</div>
   